---
- name: Copy Docker Compose file
  template:
    src: docker-compose.yml.j2
    dest: "{{ app_dir }}/docker-compose.yml"
    owner: "{{ user }}"
    group: "{{ group }}"
    mode: '0644'
  tags: [docker, config]
